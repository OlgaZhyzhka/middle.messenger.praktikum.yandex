const tpl = `{{{ avatar }}} <div class="contact__info"><span class="contact__title title">{{ title }}</span><span class="contact__message">{{ last_message.content }}</span></div>`;
export default tpl;
