const tpl = `<img src="{{ imgSrc }}" alt="{{ imgAlt }}">
  <h1 class="page__heading page__heading_lg">{{ pageTitle }}</h1>
  <h4 class='page__title page__title_lg'>not found</h4>{{{ link }}}`;

export default tpl;
